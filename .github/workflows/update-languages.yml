name: Update Languages Card

on:
  schedule:
    # Run daily (languages don't change as frequently)
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger
  repository_dispatch:
    types: [update-languages]

jobs:
  generate-languages:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PyGithub

      - name: Generate languages card
        env:
          # GITHUB_TOKEN has access to private repos in Actions
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python generate_languages.py

      - name: Commit and push if changed
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add languages.svg
          git diff --staged --quiet || git commit -m "Update languages card [skip ci]"
          git push
